for x in width
	for y in height
		construct ro and rd
		normalize rd
		for object in objects
			t = object intersection
			keep the min t val
			keep the corresponding object
			use the object's color for the pixel color
			need to know the specular object color
			need to know the normal at the intersection
			I = (0,0,0)
			for light in lights
				L = construct a ray form the intersection to the light
				for object in objects
					t = object intersection
					if t < distance to L then it's in shadow.
					break when finding a shadow
				if shadow: continue
				else: light the scene
				radial attenuation: applies to all lights
				angular attenuation: just spot lights
				
				N = form file for planes
				N = intersection point - sphere center for sphere
				
				V = view vector (-Rd)
				R = reflection of L about N
				
				i_diff = O_diff * I_i (N . L) if N.L > 0
				i_spec O_spec * I_i (V . R)^2 if V.R > 0 && N.L > 0
				
				I += f_rad + f_ang + I_i * (O_spec + O_diff)
			clamp(i, (i,i,i)) (if the values are greater than 1, set to 1 for rgb)
			assign the color to an element in the pixmap